<h2>Carrito</h2>

<ul>
    {{#each cart.products}}
        <li>
                    <h2>{{this.title}}</h2>
                    <p>Precio: ${{this.price}}</p>
                    <p>Cantidad: {{this.quantity}}</p>
                </li>
    {{/each}}
</ul>
